// Money Counter.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <algorithm>
using namespace std;
int main()
{
	double number;
	string type;
	while (cin >> number >> type){
		string dollar = number;
		string quarter = number * 4;
		string dime = number * 10;
		if (number > 1)

		else if (number == 1)
	}
	return 0;
}

